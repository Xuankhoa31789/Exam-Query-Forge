package com.eqf.model;

/** 4 mức độ khó theo ma trận đề: nhận biết / thông hiểu / vận dụng / vận dụng cao. */
public enum DifficultyLevel {
    RECOGNITION,
    COMPREHENSION,
    APPLICATION,
    HIGH_APPLICATION
}
